#!/usr/bin/perl

# test peso execution

# ---   *   ---   *   ---
# deps

  use v5.36.0;
  use strict;
  use warnings;

  use English qw(-no_match_vars);

  use lib $ENV{'ARPATH'}.'/avtomat/';

  use style;
  use arstd;

  use peso::ipret;

# ---   *   ---   *   ---

my $rd=peso::ipret::run(glob('~/cltest'));
peso::ipret::xpile_c($rd);

# ---   *   ---   *   ---
