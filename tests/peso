#!/usr/bin/perl

# test peso classes

# ---   *   ---   *   ---
# deps

  use v5.36.0;
  use strict;
  use warnings;

  use English qw(-no_match_vars);

  use lib $ENV{'ARPATH'}.'/avtomat/sys/';
  use lib $ENV{'ARPATH'}.'/avtomat/hacks/';
  use lib $ENV{'ARPATH'}.'/avtomat/';

  use Style;
  use Arstd;

  use Fmat;

  use Peso::Blk;

# ---   *   ---   *   ---

my $types={

  unit=>{size=>0x10},
  half=>{size=>0x08},

};

my $mem=[];
my $frame=Peso::Blk->new_frame($types);

my $non=$frame->{non};

$non->grow(32);

my $beg=$non->baptize(0,16,'beg');

${$beg->[0]}=0xDEADBEEF;

$non->prich();

# ---   *   ---   *   ---
